mixin breadcrumbs(crumbs)
  // BREADCRUMBS
  .breadcrumbs
    ul.breadcrumbs__list
      each crumb, index in crumbs
        li
          if (index !== crumbs.length - 1)
            a(href="#")!= crumb
          else
            span!= crumb
